
<form method="POST" action="{{ path('get_resturant') }}"> 
  <div class="mb-3">
    <div class="form-group">
        <label for="multiselectField">Select Options:</label>
        <select multiple class="form-control" id="multiselectField" name="multiselectField[]">
            <option value="option1">Option 1</option>
            <option value="option2">Option 2</option>
            <option value="option3">Option 3</option>
            <!-- Add more options as needed -->
        </select>
    </div>
  </div>
  <button type="submit" class="btn btn-primary">Submit</button>
  <button class="btn btn-primary"><a href="{{ path('selected_choices_list') }}" class="btn btn-primary">show all selected choices</a></button>
</form>